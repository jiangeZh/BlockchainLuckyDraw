# BlockchainLuckyDraw

##安装MongoDB

去 官网www.mongodb.org/downloads 下载对应系统的 MongoDB 压缩包。解压后将文件夹重命名为 mongodb，并在 mongodb 文件夹里新建 BlockchainLuckyDraw 文件夹作为我们抽奖系统内容的存储目录。进入到 bin 目录下：运行：

./mongod --dbpath ../BlockchainLuckyDraw/

以上命令的意思是:设置 BlockchainLuckyDraw 文件夹作为我们工程的存储目录并启动数据库。
